"use strict";

// 문자열이 변수와 연결된 메모리 영역에 저장됨!

// 함수형 프로그래밍 언어는 변수값 변경을 금지!
// 상수 처럼 사용한다

// 변수 명 규칙
// 첫글자는 숫자 안되고, 문자 숫자 $ _ 만 이름으로 가능!
//
let $ = 1;
let _ = 2;
alert($ + _);

// use strict를 사용 안하면 let없이 선언해도 변수가 할당된다!
num = 5;
alert(num);

// const는 상수
//let;
