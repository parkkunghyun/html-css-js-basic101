let name = prompt("자바스크립트의 '공식' 이름은 무엇일까요?", "");
if (name == "ECMAScript") {
  alert("정답입니다!");
} else {
  alert("모르셨나요?\n 정답은 ECMAScript입니다!");
}

let a = 3;
let b = -1;
result = a + b < 4 ? "미만" : "이상";

let message;

login == "직원"
  ? "안녕하세요"
  : login == "임원"
  ? "환영합니다"
  : login == ""
  ? "로그인이 필요합니다"
  : "";
